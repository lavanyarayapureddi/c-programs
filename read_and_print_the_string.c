#include<stdio.h>
#include<string.h>
void main()
{
    char str[100];
    int size;
    printf("enter the size of a string:");
    scanf("%d",&size);
    printf("enter the str:");
    scanf("%s",str);
    printf("%s",str);
}
